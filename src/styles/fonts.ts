import { Orbitron, Inter, Roboto_Mono } from '@fontsource';

export const fonts = {
  orbitron: 'Orbitron',
  inter: 'Inter',
  mono: 'Roboto Mono',
};

// Usage: Add <link> tags for Google Fonts in index.html if not using @fontsource
// Or import '@fontsource/inter', etc. in your main entry point.
